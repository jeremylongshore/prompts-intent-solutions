# Legacy Prompt Archive

This directory preserves the previous generation of prompts before the 2025-10-16 overhaul.

- Original category folders (business, development, specialized) now live under `prompts/`.
- Use these only for historical reference or to recover context while migrating engagements.
- When deprecating a modern prompt, add an after-action note and move it here with the same structure.

For current production prompts, see the flat `prompts/` directory in the repository root.*** End Patch
